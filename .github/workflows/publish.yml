name: Publish

on:
  push:
    tags:
      - "v*.*.*"

permissions: {}

concurrency:
  group: npm-publish-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publish package
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22.19.0
          registry-url: https://registry.npmjs.org/
          cache: npm

      - name: Use npm 11
        run: npm install --global npm@11

      - name: Show tool versions
        run: |
          node --version
          npm --version

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Tool smoke
        run: npm run test:tools

      - name: Verify tag matches package version
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          EXPECTED_TAG="v${VERSION}"

          if [ "${EXPECTED_TAG}" != "${GITHUB_REF_NAME}" ]; then
            echo "Tag ${GITHUB_REF_NAME} does not match package.json version ${VERSION}. Expected ${EXPECTED_TAG}."
            exit 1
          fi

          if [[ "${VERSION}" == *-* ]]; then
            echo "Pre-release publishing requires an explicit npm dist-tag and is not handled by this workflow."
            exit 1
          fi

      - name: Verify version is not already published
        run: |
          PACKAGE_NAME="$(node -p "require('./package.json').name")"
          VERSION="$(node -p "require('./package.json').version")"

          if npm view "${PACKAGE_NAME}@${VERSION}" version >/dev/null 2>&1; then
            echo "${PACKAGE_NAME}@${VERSION} is already published."
            exit 1
          fi

      - name: Verify package contents
        run: npm pack --dry-run --json

      - name: Publish to npm
        run: npm publish
