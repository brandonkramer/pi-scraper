# Architecture boundary: src/tui/ must never import from src/tools/
id: no-tui-tools-import
language: TypeScript
severity: error
message: "src/tui/ must not import from src/tools/. Move shared code to an internal module."
rule:
  kind: import_statement
  regex: tools/
files:
  - "src/tui/**/*.ts"
