# Install smoke

Packs the local package, installs it into a temporary project, and verifies the Pi manifest and public web tool registration.

| Script      | Command                 | Output                                      |
| ----------- | ----------------------- | ------------------------------------------- |
| `smoke.mjs` | `npm run smoke:install` | Console-only; temporary project is deleted. |
